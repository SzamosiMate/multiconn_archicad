# This file is automatically generated by the build system.
# Do not edit this file directly.

from __future__ import annotations
from typing import TYPE_CHECKING
from pydantic import TypeAdapter

from multiconn_archicad.models.tapir.commands import (
    CreateKeynoteFoldersParameters,
    CreateKeynoteFoldersResult,
    CreateKeynoteItemsParameters,
    CreateKeynoteItemsResult,
    CreateKeynoteLabelsParameters,
    CreateKeynoteLabelsResult,
    DeleteKeynoteFoldersParameters,
    DeleteKeynoteFoldersResult,
    DeleteKeynoteItemsParameters,
    DeleteKeynoteItemsResult,
    GetKeynoteAutoTextsParameters,
    GetKeynoteAutoTextsResult,
    GetKeynoteTreeResult,
    ModifyKeynoteFoldersParameters,
    ModifyKeynoteFoldersResult,
    ModifyKeynoteItemsParameters,
    ModifyKeynoteItemsResult,
)
from multiconn_archicad.models.tapir.types import (
    ElementIdArrayItem,
    ErrorItem,
    FailedExecutionResult,
    KeynoteAutoTextTokens,
    KeynoteFolderData,
    KeynoteFolderIdArrayItem,
    KeynoteFolderModificationData,
    KeynoteItemData,
    KeynoteItemIdArrayItem,
    KeynoteItemModificationData,
    KeynoteLabelData,
    SuccessfulExecutionResult,
)

if TYPE_CHECKING:
    from multiconn_archicad.core.core_commands import CoreCommands


class KeynoteCommands:
    def __init__(self, core: CoreCommands):
        self._core = core

    def create_keynote_folders(
        self, folders_data: list[KeynoteFolderData]
    ) -> list[ErrorItem | KeynoteFolderIdArrayItem]:
        """
        Creates keynote folders under the given parent folders (or under the root folder).
        Available from Archicad 28.

        Args:
            folders_data (list[KeynoteFolderData]): Array of data to create keynote folders.

        Returns:
            list[ErrorItem | KeynoteFolderIdArrayItem]: A list of keynote folder identifiers or
                errors.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "foldersData": folders_data,
        }
        validated_params = CreateKeynoteFoldersParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "CreateKeynoteFolders", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = CreateKeynoteFoldersResult.model_validate(response_dict)
        return validated_response.keynoteFolderIdsOrErrors

    def create_keynote_items(self, items_data: list[KeynoteItemData]) -> list[ErrorItem | KeynoteItemIdArrayItem]:
        """
        Creates keynote items in the given parent folders (or in the root folder). Available
        from Archicad 28.

        Args:
            items_data (list[KeynoteItemData]): Array of data to create keynote items.

        Returns:
            list[ErrorItem | KeynoteItemIdArrayItem]: A list of keynote item identifiers or
                errors.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "itemsData": items_data,
        }
        validated_params = CreateKeynoteItemsParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "CreateKeynoteItems", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = CreateKeynoteItemsResult.model_validate(response_dict)
        return validated_response.keynoteItemIdsOrErrors

    def create_keynote_labels(self, labels_data: list[KeynoteLabelData]) -> list[ElementIdArrayItem | ErrorItem]:
        """
        Creates Label elements that reference the given keynote items via autotext. Available
        from Archicad 28.

        Args:
            labels_data (list[KeynoteLabelData]): Array of data to create keynote labels.

        Returns:
            list[ElementIdArrayItem | ErrorItem]: A list of element identifiers or errors.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "labelsData": labels_data,
        }
        validated_params = CreateKeynoteLabelsParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "CreateKeynoteLabels", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = CreateKeynoteLabelsResult.model_validate(response_dict)
        return validated_response.elements

    def delete_keynote_folders(
        self, keynote_folder_ids: list[KeynoteFolderIdArrayItem]
    ) -> list[FailedExecutionResult | SuccessfulExecutionResult]:
        """
        Deletes the given keynote folders including their content. Available from Archicad 28.

        Args:
            keynote_folder_ids (list[KeynoteFolderIdArrayItem]): The keynote folders to delete.

        Returns:
            list[FailedExecutionResult | SuccessfulExecutionResult]: A list of execution
                results.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "keynoteFolderIds": keynote_folder_ids,
        }
        validated_params = DeleteKeynoteFoldersParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "DeleteKeynoteFolders", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = DeleteKeynoteFoldersResult.model_validate(response_dict)
        return validated_response.executionResults

    def delete_keynote_items(
        self, keynote_item_ids: list[KeynoteItemIdArrayItem]
    ) -> list[FailedExecutionResult | SuccessfulExecutionResult]:
        """
        Deletes the given keynote items. Available from Archicad 28.

        Args:
            keynote_item_ids (list[KeynoteItemIdArrayItem]): The keynote items to delete.

        Returns:
            list[FailedExecutionResult | SuccessfulExecutionResult]: A list of execution
                results.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "keynoteItemIds": keynote_item_ids,
        }
        validated_params = DeleteKeynoteItemsParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "DeleteKeynoteItems", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = DeleteKeynoteItemsResult.model_validate(response_dict)
        return validated_response.executionResults

    def get_keynote_auto_texts(
        self, keynote_items: list[KeynoteItemIdArrayItem]
    ) -> list[ErrorItem | KeynoteAutoTextTokens]:
        """
        Retrieves the autotext tokens of the given keynote items. The tokens can be used as
        label text content to reference the fields of a keynote item. Available from Archicad
        28.

        Args:
            keynote_items (list[KeynoteItemIdArrayItem]): The keynote items to get the autotext
                tokens for.

        Returns:
            list[ErrorItem | KeynoteAutoTextTokens]: A list of keynote autotext tokens or
                errors.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "keynoteItems": keynote_items,
        }
        validated_params = GetKeynoteAutoTextsParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "GetKeynoteAutoTexts", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = GetKeynoteAutoTextsResult.model_validate(response_dict)
        return validated_response.autoTexts

    def get_keynote_tree(self) -> GetKeynoteTreeResult:
        """
        Retrieves the whole keynote folder and item hierarchy. The technical root folder is not
        included in the output; the top-level folders and items are returned directly. Available
        from Archicad 28.

        Returns:
            GetKeynoteTreeResult

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        response_dict = self._core.post_tapir_command("GetKeynoteTree")
        validated_response = GetKeynoteTreeResult.model_validate(response_dict)
        return validated_response

    def modify_keynote_folders(
        self, folders_data: list[KeynoteFolderModificationData]
    ) -> list[FailedExecutionResult | SuccessfulExecutionResult]:
        """
        Modifies the key, title or reference of the given keynote folders. Available from
        Archicad 28.

        Args:
            folders_data (list[KeynoteFolderModificationData]): Array of data to modify keynote
                folders. Only provided fields are changed.

        Returns:
            list[FailedExecutionResult | SuccessfulExecutionResult]: A list of execution
                results.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "foldersData": folders_data,
        }
        validated_params = ModifyKeynoteFoldersParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "ModifyKeynoteFolders", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = ModifyKeynoteFoldersResult.model_validate(response_dict)
        return validated_response.executionResults

    def modify_keynote_items(
        self, items_data: list[KeynoteItemModificationData]
    ) -> list[FailedExecutionResult | SuccessfulExecutionResult]:
        """
        Modifies the key, title, description or reference of the given keynote items. Available
        from Archicad 28.

        Args:
            items_data (list[KeynoteItemModificationData]): Array of data to modify keynote
                items. Only provided fields are changed.

        Returns:
            list[FailedExecutionResult | SuccessfulExecutionResult]: A list of execution
                results.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "itemsData": items_data,
        }
        validated_params = ModifyKeynoteItemsParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "ModifyKeynoteItems", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = ModifyKeynoteItemsResult.model_validate(response_dict)
        return validated_response.executionResults
