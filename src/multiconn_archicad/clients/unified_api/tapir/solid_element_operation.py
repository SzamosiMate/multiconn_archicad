# This file is automatically generated by the build system.
# Do not edit this file directly.

from __future__ import annotations
from typing import TYPE_CHECKING
from pydantic import TypeAdapter

from multiconn_archicad.models.tapir.commands import (
    CreateSolidElementLinksParameters,
    CreateSolidElementLinksResult,
    GetSolidElementLinksParameters,
    GetSolidElementLinksResult,
    RemoveSolidElementLinksParameters,
    RemoveSolidElementLinksResult,
)
from multiconn_archicad.models.tapir.types import (
    ElementIdArrayItem,
    FailedExecutionResult,
    SolidLinkData,
    SolidLinkReference,
    SolidLinksOfElement,
    SuccessfulExecutionResult,
)

if TYPE_CHECKING:
    from multiconn_archicad.core.core_commands import CoreCommands


class SolidElementOperationCommands:
    def __init__(self, core: CoreCommands):
        self._core = core

    def create_solid_element_links(
        self, solid_links: list[SolidLinkData]
    ) -> list[FailedExecutionResult | SuccessfulExecutionResult]:
        """
        Creates solid element operation links between target and operator elements.

        Args:
            solid_links (list[SolidLinkData]): List of solid element operation links to create.

        Returns:
            list[FailedExecutionResult | SuccessfulExecutionResult]: A list of execution
                results.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "solidLinks": solid_links,
        }
        validated_params = CreateSolidElementLinksParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "CreateSolidElementLinks", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = CreateSolidElementLinksResult.model_validate(response_dict)
        return validated_response.executionResults

    def get_solid_element_links(self, elements: list[ElementIdArrayItem]) -> list[SolidLinksOfElement]:
        """
        Returns solid element operation links for each queried element, grouped by role (target
        or operator).

        Args:
            elements (list[ElementIdArrayItem]): Elements to query. Returns all solid links
                where each element is a target or an operator.

        Returns:
            list[SolidLinksOfElement]: For each input element, the solid links where it acts as
                target and where it acts as operator.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "elements": elements,
        }
        validated_params = GetSolidElementLinksParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "GetSolidElementLinks", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = GetSolidElementLinksResult.model_validate(response_dict)
        return validated_response.solidLinks

    def remove_solid_element_links(
        self, solid_links: list[SolidLinkReference]
    ) -> list[FailedExecutionResult | SuccessfulExecutionResult]:
        """
        Removes solid element operation links between target and operator elements.

        Args:
            solid_links (list[SolidLinkReference]): List of solid element operation links to
                remove.

        Returns:
            list[FailedExecutionResult | SuccessfulExecutionResult]: A list of execution
                results.

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "solidLinks": solid_links,
        }
        validated_params = RemoveSolidElementLinksParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "RemoveSolidElementLinks", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = RemoveSolidElementLinksResult.model_validate(response_dict)
        return validated_response.executionResults
