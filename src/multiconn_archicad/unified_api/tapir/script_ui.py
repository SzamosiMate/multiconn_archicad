# This file is automatically generated by the build system.
# Do not edit this file directly.

from __future__ import annotations
from typing import TYPE_CHECKING
from pydantic import TypeAdapter

from multiconn_archicad.models.tapir.commands import GetScriptUIResultResult, ShowScriptUIParameters, ShowScriptUIResult
from multiconn_archicad.models.tapir.types import FailedExecutionResult, SuccessfulExecutionResult

if TYPE_CHECKING:
    from multiconn_archicad.core.core_commands import CoreCommands


class ScriptUiCommands:
    def __init__(self, core: CoreCommands):
        self._core = core

    def get_script_ui_result(self) -> GetScriptUIResultResult:
        """
        Retrieves and clears the result last submitted from the Script UI palette's page (via
        window.ACAPI.SubmitResult), if any.

        Returns:
            GetScriptUIResultResult

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        response_dict = self._core.post_tapir_command("GetScriptUIResult")
        validated_response = GetScriptUIResultResult.model_validate(response_dict)
        return validated_response

    def show_script_ui(
        self,
        html_content: str,
        width: None | int = None,
        height: None | int = None,
        title: None | str = None,
        resizable: None | bool = None,
        zoom_enabled: None | bool = None,
        zoom_level: None | float = None,
        scroll_bars_visible: None | bool = None,
        context_menu_enabled: None | bool = None,
        navigation_disabled: None | bool = None,
        allow_self_signed_certificates: None | bool = None,
        clear_cookies: None | bool = None,
        auto_height: None | bool = None,
    ) -> FailedExecutionResult | SuccessfulExecutionResult:
        """
        Shows the given HTML content in a native Archicad palette (a tkinter alternative for
        Python scripts).

        Args:
            html_content (str): The full HTML document to display in the Script UI palette.
            width (None | int): Optional palette client width in pixels. Leave unset to keep the
                current size.
            height (None | int): Optional palette client height in pixels. Leave unset to keep
                the current size.
            title (None | str): Optional palette window title. Leave unset to keep the current
                title.
            resizable (None | bool): Whether the user can drag-resize the palette. Defaults to
                true.
            zoom_enabled (None | bool): Whether Ctrl+scroll/pinch zooming is allowed in the
                page. Defaults to true.
            zoom_level (None | float): Optional initial zoom level (1.0 = 100%). Leave unset to
                keep the current zoom.
            scroll_bars_visible (None | bool): Whether the browser's own scrollbars are shown.
                Defaults to true.
            context_menu_enabled (None | bool): Whether right-click shows the browser's context
                menu (e.g. Inspect Element). Defaults to true.
            navigation_disabled (None | bool): Prevents the page from navigating away (e.g.
                clicking a link to another site). Defaults to false.
            allow_self_signed_certificates (None | bool): Only relevant if htmlContent links to
                a remote https:// resource with a self-signed certificate. Defaults to false.
            clear_cookies (None | bool): Deletes all browser cookies before loading htmlContent.
                Defaults to false.
            auto_height (None | bool): Automatically resizes the palette's height to fit the
                page content as it changes. Defaults to false.

        Returns:
            FailedExecutionResult | SuccessfulExecutionResult

        Raises:
            ArchicadAPIError: If the API returns an error response.
            RequestError: If there is a network or connection error.
            pydantic.ValidationError: If the parameters, or the API Response fail validation.
        """
        params_dict = {
            "htmlContent": html_content,
            "width": width,
            "height": height,
            "title": title,
            "resizable": resizable,
            "zoomEnabled": zoom_enabled,
            "zoomLevel": zoom_level,
            "scrollBarsVisible": scroll_bars_visible,
            "contextMenuEnabled": context_menu_enabled,
            "navigationDisabled": navigation_disabled,
            "allowSelfSignedCertificates": allow_self_signed_certificates,
            "clearCookies": clear_cookies,
            "autoHeight": auto_height,
        }
        validated_params = ShowScriptUIParameters(**params_dict)
        response_dict = self._core.post_tapir_command(
            "ShowScriptUI", validated_params.model_dump(mode="json", by_alias=True, exclude_none=True)
        )
        validated_response = TypeAdapter(ShowScriptUIResult).validate_python(response_dict)
        return validated_response
